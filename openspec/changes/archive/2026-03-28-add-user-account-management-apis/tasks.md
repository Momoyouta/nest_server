## 1. DTO 与接口契约

- [x] 1.1 在用户模块新增/更新用户端资料编辑相关请求 DTO（基本信息、修改密码、修改头像、发送手机号验证码、提交手机号修改），补齐 class-validator 校验规则。
- [x] 1.2 为上述 DTO 与响应结构补齐 Swagger @ApiProperty 注解，确保字段类型、示例与必填约束完整。

## 2. 用户服务实现

- [x] 2.1 在用户服务实现修改基本信息逻辑，仅允许更新受控字段（当前为 gender），并维护 update_time。
- [x] 2.2 在用户服务实现修改密码逻辑：校验旧密码正确后再写入新密码哈希。
- [x] 2.3 在用户服务实现修改头像逻辑：校验 tempAvatarPath，移动文件到 fileStore/users/avatars/{user_id}.png（存在则覆盖），并更新 user.avatar。
- [x] 2.4 在用户服务实现手机号验证码逻辑：生成 6 位验证码写入 Redis（TTL 300 秒），控制台输出验证码。
- [x] 2.5 在用户服务实现手机号修改逻辑：校验 Redis 验证码后更新手机号并删除验证码键，处理手机号冲突与验证码错误场景。

## 3. 控制器与鉴权边界

- [x] 3.1 在现有用户控制器新增用户端接口路由（基本信息、密码、头像、修改手机号；其中修改手机号接口支持发送验证码与校验更新），使用用户 JWT 鉴权，不接入 AdminAuth。
- [x] 3.2 为新增用户端接口补齐 Swagger 控制器注解（@ApiTags、@ApiOperation、@ApiBody、@ApiOkResponse、@ApiBadRequestResponse）。
- [x] 3.3 校验并确认管理员端受限接口保持原有 AdminAuth 语义，不受本次新增接口影响。

## 4. 联调与文档收口

- [x] 4.1 完成接口自测与联调：覆盖旧密码错误、验证码过期、头像路径非法、未登录访问等关键失败场景。
- [x] 4.2 更新必要的模块说明与 OpenAPI 展示项，确保前端可直接按文档完成接入。